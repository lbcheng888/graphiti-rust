## Graphiti MCP Server environment variables

# Project and data isolation (optional; default uses current working directory)
# GRAPHITI_PROJECT=.
# GRAPHITI_DATA_DIR=.graphiti/data

# Force using explicit --config path even if project .graphiti/config.toml exists
# GRAPHITI_CONFIG_FORCE=1

# Disable initial project scan to avoid blocking stdio startup
# GRAPHITI_ENABLE_INITIAL_SCAN=0

# Logging
RUST_LOG=info,graphiti=debug

# Embedding model location (preferred way to avoid hardcoded paths)
# Put local HF model weights (tokenizer.json, config.json, *.safetensors) under this directory
EMBEDDING_MODEL_DIR=/Users/yourname/embeddinggemma-300m

# Offline mode for Hugging Face
HF_HUB_OFFLINE=1

